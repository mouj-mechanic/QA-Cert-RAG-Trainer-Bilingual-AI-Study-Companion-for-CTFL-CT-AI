# Chat_ISTQB — Unofficial AI Study Companion
# Target runtime: Python 3.11 (Streamlit Community Cloud → Advanced → Python 3.11)
#
# chromadb is pinned EXACTLY: the same version MUST create and read vector_db/.

streamlit==1.63.0
pypdf==6.18.1
chromadb==1.5.9
sentence-transformers==6.0.1
torch==2.14.0
torchvision==0.29.0
openai==3.13.0
python-dotenv==1.2.3
numpy==2.4.6

# Chroma on Streamlit Community Cloud (Linux): modern SQLite via pysqlite3
pysqlite3-binary==0.5.4; platform_system == "Linux"

# Dev / CI (also harmless on Cloud)
pytest==9.1.1
